import { Component } from '@angular/core';

@Component({
  selector: 'app-home',
  templateUrl: './home.component.html',  // Asegúrate de que el nombre y la ruta sean correctos
  styleUrls: ['./home.component.css']    // Asegúrate de que el nombre y la ruta sean correctos
})
export class HomeComponent { }

